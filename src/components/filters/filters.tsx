// eslint-disable-next-line no-use-before-define
import React from 'react';
import './filters.scss';

const Filters = () => {
  return (
    <div>
      <p>Filters</p>
    </div>
  );
};

export default Filters;
